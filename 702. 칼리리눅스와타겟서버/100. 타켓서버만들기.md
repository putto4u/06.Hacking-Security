## 타켓서버만들기

설치 계획
1. 우분투 14.04.6 desktop    ** cli 터미널 모드 변경  ctrl + Alt +F3
2. 아파치 2.2.8
3. mysql 5.0.51a 


## 1. 게이트웨이를 가져오지 못할 때  
sudo vi /etc/network/interfaces

```
# 로컬 루프백 인터페이스 (기존 내용 유지)
auto lo
iface lo inet loopback

# 고정 IP 설정 (eth0 기준)
auto eth0
iface eth0 inet static
    address 192.168.10.1xx       # 할당할 고정 IP 주소
    netmask 255.255.255.0       # 서브넷 마스크 (Subnet Mask)
    gateway 192.168.10.1         # 게이트웨이 (Gateway) 주소
    dns-nameservers 8.8.8.8 8.8.4.4  # DNS 서버 주소 (Google DNS 예시)
```

dns 서버 설정
```
$ sudo vi /etc/resolv.conf
#### 기존에 등록된 nameserver 정상적인 아이피로 있다면 추가할 필요없음
#### 예) 168.126.63.1  한국 KR DNS 서버(정상, 가장 이상적임)가 이미 있다면 아래줄은 추가할 필요 없음

## 없다면 아래줄 추가, 하나만 추가해도 됩니다. 
nameserver 8.8.8.8
nameserver 8.8.4.4
```
리부팅 후 외부에서 타겟서버로 ping 테스트가 성공하면 ok!

2. ssh 서버 설치 및 시작
```
sudo apt-get install openssh-server
service ssh status
sudo service ssh start
# sudo service ssh restart
sudo update-rc.d ssh defaults  # 부팅시 자동 서비스
```

3. xshell 연결하기
   

4. 리눅스 버전 고정 하기
```
# 1. 현재 사용 중인 커널 관련 패키지들 고정 (가장 중요)
sudo apt-mark hold linux-image-$(uname -r)               # OS 커널 이미지 고정
sudo apt-mark hold linux-headers-$(uname -r)             # 커널 개발 헤더 고정
sudo apt-mark hold linux-image-extra-$(uname -r)         # 시스템 버전정보 

sudo apt-mark hold base-files  ## 2. 우분투 배포판 버전 정보를 담은 패키지 고정
```

5. 옛날 버전 저장소 주소 변경
```
# 저장소 주소를 old-releases로 변경
sudo sed -i 's/archive.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list
sudo sed -i 's/security.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list
sudo sed -i 's/kr.archive.ubuntu.com/old-releases.ubuntu.com/g' /etc/apt/sources.list
```

6. 아파치 2.2.8 설치


